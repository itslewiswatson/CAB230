import { Button } from "@material-ui/core";
import React from "react";

export const App = () => {
  return (
    <Button variant="contained" color="primary">
      Hello World
    </Button>
  );
};

export default App;
